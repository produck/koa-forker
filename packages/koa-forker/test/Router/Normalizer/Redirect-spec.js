const assert = require('assert');
const { describe, it } = require('mocha');

const normalizeRouterOptions = require('../../../src/Router/Normalizer/redirect');

describe('Router::Normalizer::Redirect()', function () {

});